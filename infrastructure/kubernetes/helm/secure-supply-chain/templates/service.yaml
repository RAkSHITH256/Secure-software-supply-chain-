apiVersion: v1
kind: Service
metadata:
  name: {{ include "secure-supply-chain.fullname" . }}
  labels:
    {{- include "secure-supply-chain.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secure-supply-chain.selectorLabels" . | nindent 4 }}
