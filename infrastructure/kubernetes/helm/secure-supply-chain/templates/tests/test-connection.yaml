apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "secure-supply-chain.fullname" . }}-test-connection"
  labels:
    {{- include "secure-supply-chain.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "secure-supply-chain.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
